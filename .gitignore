node_modules/
.env
storage/
database/*.db
database/*.db-shm
database/*.db-wal
# Certificados y claves — nunca commitear
certs/
*.pem
*.p12
*.crt
*.key
*.csr
# Script de instalacion — se distribuye por separado, no va en el repo
instalar.bat
# Logs del servidor
logs/*.log
