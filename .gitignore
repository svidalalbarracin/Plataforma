node_modules/
.env
storage/
database/*.db
database/*.db-shm
database/*.db-wal
# Certificados y claves — nunca commitear
certs/
*.pem
*.p12
*.crt
*.key
*.csr
# Scripts de instalacion — se distribuyen por separado, no van en el repo
instalar.bat
instalar.sh
# Logs del servidor
logs/*.log
